---
        - name: umounting lvm
          hosts: devops
          user: root
          connection: ssh
          tasks:

                 - name: Unmount
                   mount:
                         path: /mnt/lvm
                         state: absent










this line just for texting  purpose
